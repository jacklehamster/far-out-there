{
    "type": "dialog",
    "fontSheet": { "type": "reference", "path": "../fonts/font-sheet.json" },
    "labels": [
      { "text": "soda (40G)", "x": 7, "y": 0,
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and not scene.completed"
        },
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.message != undefined or scene.completed"
        }
      },
      { "text": "taco (100G)", "x": 7, "y": 8,
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and not scene.completed"
        },
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.message != undefined or scene.completed"
        }
      },
      { "text": "fishtaco (??)", "x": 7, "y": 16,
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and scene.secret.fishtaco != undefined and not scene.completed"
        },
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.message != undefined or not scene.secret.fishtaco or scene.completed"
        }
      },
      { "text": "exit", "x": 7, "y": 24,
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and not scene.completed"
        },
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.message != undefined or scene.completed"
        }
      },
      { "text": ">", "x": 1, "y": 0,
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.menuIndex != 0 or scene.message != undefined or scene.completed"
        },
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and not scene.completed"
        }
      },
      { "text": ">", "x": 1, "y": 8,
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.menuIndex != 1 or scene.message != undefined or scene.completed"
        },
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and not scene.completed"
        }
      },
      { "text": ">", "x": 1, "y": 16,
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.menuIndex != 2 or scene.message != undefined or scene.completed"
        },
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and not scene.completed"
        }
      },
      { "text": ">", "x": 1, "y": 24,
        "noScroll": true,
        "hidden": {
          "type": "condition",
          "formula": "scene.menuIndex != 3 or scene.message != undefined or scene.completed"
        },
        "outline": {
          "type": "condition",
          "formula": "scene.message == undefined and not scene.completed"
        }
      },
      {
        "x": 1, "y": 49,
        "width": 62, "height": 20,
        "box": true
      }
    ]
  }